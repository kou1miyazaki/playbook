- hosts: all
  remote_user: root
  sudo: yes

  vars:

  tasks:
  - name: 前処理
    include: tasks/base.yml

  - name: httpd Install
    include: tasks/httpd.yml

  - name: php Install
    include: tasks/php.yml

